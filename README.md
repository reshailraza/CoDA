# CoDA
Cross-domain adaptive batch-norm (AdaBN) domain adaptation for a CNN tomographic reconstruction model (CAFNet). Adapts a pretrained network to new measurement-domain aspect ratios without full retraining. Includes adaptation/training scripts, plotting utilities, trained models, and measurement data.
