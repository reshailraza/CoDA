# CoDA
Consistency-driven adaptation (CoDA) is presented for channel attention fusion network (CAFNet). Adapts a pretrained network to new measurement-domain aspect ratios without full retraining. Includes adaptation/training scripts, plotting utilities, trained models, and measurement data.
